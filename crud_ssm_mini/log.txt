2018-11-23:
	1、新增模块为，联合查询，员工表与部门表;
	2、applicationContext映射文件，注意包的扫描位置，sqlSessionFactory;
	3、springMVC，开启注解扫描，添加新的包的位置;
	4、mapper映射文件，多表查询，利用resultMap，建立表字段与实体类属性的有效映射，不可缺少；
	此外，在主表对应的实体类中添加从表对应实体的实例引用;
	5、路径与文件名重复问题：新增模块与原有在同一目录下时，持久层、控制层、业务层相关类与接口的名称不可重复;
	6、与miniUI框架整合后，在多表查询时，字段field="dept.name" //部门名称