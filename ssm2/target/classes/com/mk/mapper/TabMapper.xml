<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mk.mapper.TabMapper">
	<!-- <select id="selectUsers" resultType="com.mk.entry.User">
		select * from user limit #{arg0},#{arg1}
	</select>
	
	<select id="selectCountOfUser" resultType="int">
		select count(1) from user
	</select> -->
	<select id="selectAll" resultType="com.mk.entry.Tab">
		select * from suiyi 
	</select>
	<select id="selectByPage" resultType="com.mk.entry.Tab">
		select * from suiyi limit #{arg0},#{arg1}
	</select>
</mapper>